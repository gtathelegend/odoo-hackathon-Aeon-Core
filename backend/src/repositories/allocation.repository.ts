import { createPlaceholderRepository } from './_placeholder.repository';

/** Allocation repository placeholder. Prisma queries are added in a later prompt. */
export const allocationRepository = createPlaceholderRepository('allocation');
