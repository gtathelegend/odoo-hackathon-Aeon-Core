import { createPlaceholderRepository } from './_placeholder.repository';

/** Notifications repository placeholder. Prisma queries are added in a later prompt. */
export const notificationsRepository = createPlaceholderRepository('notifications');
