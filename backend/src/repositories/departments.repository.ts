import { createPlaceholderRepository } from './_placeholder.repository';

/** Departments repository placeholder. Prisma queries are added in a later prompt. */
export const departmentsRepository = createPlaceholderRepository('departments');
