import { createPlaceholderRepository } from './_placeholder.repository';

/** Assistant repository placeholder. Prisma queries are added in a later prompt. */
export const assistantRepository = createPlaceholderRepository('assistant');
