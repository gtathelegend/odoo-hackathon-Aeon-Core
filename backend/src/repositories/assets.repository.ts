import { createPlaceholderRepository } from './_placeholder.repository';

/** Assets repository placeholder. Prisma queries are added in a later prompt. */
export const assetsRepository = createPlaceholderRepository('assets');
