import { createPlaceholderRepository } from './_placeholder.repository';

/** Reports repository placeholder. Prisma queries are added in a later prompt. */
export const reportsRepository = createPlaceholderRepository('reports');
