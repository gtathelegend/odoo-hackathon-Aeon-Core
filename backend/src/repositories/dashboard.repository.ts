import { createPlaceholderRepository } from './_placeholder.repository';

/** Dashboard repository placeholder. Prisma queries are added in a later prompt. */
export const dashboardRepository = createPlaceholderRepository('dashboard');
