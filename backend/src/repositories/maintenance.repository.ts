import { createPlaceholderRepository } from './_placeholder.repository';

/** Maintenance repository placeholder. Prisma queries are added in a later prompt. */
export const maintenanceRepository = createPlaceholderRepository('maintenance');
