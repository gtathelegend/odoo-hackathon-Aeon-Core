import { createPlaceholderRepository } from './_placeholder.repository';

/** Audit repository placeholder. Prisma queries are added in a later prompt. */
export const auditRepository = createPlaceholderRepository('audit');
