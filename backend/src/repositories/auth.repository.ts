import { createPlaceholderRepository } from './_placeholder.repository';

/** Auth repository placeholder. Prisma queries are added in a later prompt. */
export const authRepository = createPlaceholderRepository('auth');
