import { createPlaceholderRepository } from './_placeholder.repository';

/** Settings repository placeholder. Prisma queries are added in a later prompt. */
export const settingsRepository = createPlaceholderRepository('settings');
