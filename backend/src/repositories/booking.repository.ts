import { createPlaceholderRepository } from './_placeholder.repository';

/** Booking repository placeholder. Prisma queries are added in a later prompt. */
export const bookingRepository = createPlaceholderRepository('booking');
