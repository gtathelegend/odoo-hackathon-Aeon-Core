import { createPlaceholderRepository } from './_placeholder.repository';

/** Users repository placeholder. Prisma queries are added in a later prompt. */
export const usersRepository = createPlaceholderRepository('users');
