import { createPlaceholderService } from './_placeholder.service';

/** Booking service placeholder. Business logic is implemented in a later prompt. */
export const bookingService = createPlaceholderService('booking');
