import { createPlaceholderService } from './_placeholder.service';

/** Users service placeholder. Business logic is implemented in a later prompt. */
export const usersService = createPlaceholderService('users');
