import { createPlaceholderService } from './_placeholder.service';

/** Auth service placeholder. Business logic is implemented in a later prompt. */
export const authService = createPlaceholderService('auth');
