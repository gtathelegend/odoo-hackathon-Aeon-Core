import { createPlaceholderService } from './_placeholder.service';

/** Audit service placeholder. Business logic is implemented in a later prompt. */
export const auditService = createPlaceholderService('audit');
