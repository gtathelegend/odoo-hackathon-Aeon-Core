import { createPlaceholderService } from './_placeholder.service';

/** Assistant service placeholder. Business logic is implemented in a later prompt. */
export const assistantService = createPlaceholderService('assistant');
