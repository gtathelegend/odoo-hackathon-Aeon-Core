import { createPlaceholderService } from './_placeholder.service';

/** Notifications service placeholder. Business logic is implemented in a later prompt. */
export const notificationsService = createPlaceholderService('notifications');
