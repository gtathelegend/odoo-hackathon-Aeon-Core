import { createPlaceholderService } from './_placeholder.service';

/** Departments service placeholder. Business logic is implemented in a later prompt. */
export const departmentsService = createPlaceholderService('departments');
