import { createPlaceholderService } from './_placeholder.service';

/** Allocation service placeholder. Business logic is implemented in a later prompt. */
export const allocationService = createPlaceholderService('allocation');
