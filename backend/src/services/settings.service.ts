import { createPlaceholderService } from './_placeholder.service';

/** Settings service placeholder. Business logic is implemented in a later prompt. */
export const settingsService = createPlaceholderService('settings');
