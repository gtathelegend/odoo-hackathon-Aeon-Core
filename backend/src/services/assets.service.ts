import { createPlaceholderService } from './_placeholder.service';

/** Assets service placeholder. Business logic is implemented in a later prompt. */
export const assetsService = createPlaceholderService('assets');
