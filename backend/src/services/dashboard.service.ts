import { createPlaceholderService } from './_placeholder.service';

/** Dashboard service placeholder. Business logic is implemented in a later prompt. */
export const dashboardService = createPlaceholderService('dashboard');
