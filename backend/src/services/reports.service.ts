import { createPlaceholderService } from './_placeholder.service';

/** Reports service placeholder. Business logic is implemented in a later prompt. */
export const reportsService = createPlaceholderService('reports');
