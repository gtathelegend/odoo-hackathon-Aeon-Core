import { createPlaceholderController } from './_placeholder.controller';

/** Audit controller placeholder. Handlers are implemented in a later prompt. */
export const auditController = createPlaceholderController('audit');
