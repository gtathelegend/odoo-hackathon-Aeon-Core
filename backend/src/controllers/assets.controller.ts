import { createPlaceholderController } from './_placeholder.controller';

/** Assets controller placeholder. Handlers are implemented in a later prompt. */
export const assetsController = createPlaceholderController('assets');
