import { createPlaceholderController } from './_placeholder.controller';

/** Notifications controller placeholder. Handlers are implemented in a later prompt. */
export const notificationsController = createPlaceholderController('notifications');
