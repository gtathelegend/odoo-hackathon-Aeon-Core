import { createPlaceholderController } from './_placeholder.controller';

/** Users controller placeholder. Handlers are implemented in a later prompt. */
export const usersController = createPlaceholderController('users');
