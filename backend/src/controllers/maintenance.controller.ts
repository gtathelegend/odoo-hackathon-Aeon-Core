import { createPlaceholderController } from './_placeholder.controller';

/** Maintenance controller placeholder. Handlers are implemented in a later prompt. */
export const maintenanceController = createPlaceholderController('maintenance');
