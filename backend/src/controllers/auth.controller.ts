import { createPlaceholderController } from './_placeholder.controller';

/**
 * Auth controller placeholder. Real handlers (login, register, refresh,
 * logout, forgot-password) are implemented in a later prompt.
 */
export const authController = createPlaceholderController('auth');
