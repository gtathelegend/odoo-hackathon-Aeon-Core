import { createPlaceholderController } from './_placeholder.controller';

/** Dashboard controller placeholder. Handlers are implemented in a later prompt. */
export const dashboardController = createPlaceholderController('dashboard');
