import { createPlaceholderController } from './_placeholder.controller';

/** Assistant (AI) controller placeholder. Handlers are implemented in a later prompt. */
export const assistantController = createPlaceholderController('assistant');
