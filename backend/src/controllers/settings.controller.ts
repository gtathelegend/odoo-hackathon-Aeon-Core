import { createPlaceholderController } from './_placeholder.controller';

/** Settings controller placeholder. Handlers are implemented in a later prompt. */
export const settingsController = createPlaceholderController('settings');
