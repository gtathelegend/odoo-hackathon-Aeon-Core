import { createPlaceholderController } from './_placeholder.controller';

/** Departments controller placeholder. Handlers are implemented in a later prompt. */
export const departmentsController = createPlaceholderController('departments');
