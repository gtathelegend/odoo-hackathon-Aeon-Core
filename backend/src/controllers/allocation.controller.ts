import { createPlaceholderController } from './_placeholder.controller';

/** Allocation controller placeholder. Handlers are implemented in a later prompt. */
export const allocationController = createPlaceholderController('allocation');
