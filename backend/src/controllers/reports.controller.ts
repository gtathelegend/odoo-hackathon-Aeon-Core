import { createPlaceholderController } from './_placeholder.controller';

/** Reports controller placeholder. Handlers are implemented in a later prompt. */
export const reportsController = createPlaceholderController('reports');
