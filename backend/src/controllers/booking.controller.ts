import { createPlaceholderController } from './_placeholder.controller';

/** Booking controller placeholder. Handlers are implemented in a later prompt. */
export const bookingController = createPlaceholderController('booking');
