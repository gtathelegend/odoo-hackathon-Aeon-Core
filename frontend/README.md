# AssetFlow Frontend

This is the Vercel-targeted frontend shell for AssetFlow.

## Deployment

- Create a Vercel project using `frontend` as the Root Directory.
- Set `NEXT_PUBLIC_API_BASE_URL` to the public Render backend URL.
- Vercel will detect Next.js automatically.

## Local run

```bash
npm install
npm run dev
```
